{
  "name": "incubed-profile",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "5.0.1",
    "lucide-react": "^0.263.0"
  },
  "scripts": {
    "start": "react-scripts start",
    // 「prebuild」會在執行 npm run build 之前先跑，用來修正 react-scripts 的執行權限
    "prebuild": "chmod +x node_modules/.bin/react-scripts",
    // 真正的 build 指令
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not op_mini all"
  ]
}
