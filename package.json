{
  "name": "incubed-profile",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "5.0.1",
    "lucide-react": "^0.263.0"
  },
  "scripts": {
-   "start": "react-scripts start",
-   "build": "react-scripts build",
+   "start": "react-scripts start",
+   // prebuild 先把 react-scripts 二進位檔加上可執行權限
+   "prebuild": "chmod +x node_modules/.bin/react-scripts",
+   // 然後才真正執行 build
+   "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not op_mini all"
  ]
}
